# Allgemeine Proxy Einstellungen
ProxyRequests Off
<Proxy *>
      Order deny,allow
      Allow from all
</Proxy>

# Weiterleitungen nextcloud
ProxyPass /cloud http://nextcloud
ProxyPassReverse /nextcloud http://nextcloud